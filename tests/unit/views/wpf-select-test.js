import { moduleFor, test } from 'ember-qunit';

moduleFor('view:wpf-select', 'Unit | View | wpf select');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
